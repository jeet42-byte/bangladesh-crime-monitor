# ---------------------------------------------------------------------------
# Bangladesh Crime Monitor - backend runtime dependencies
# Python 3.11+
# ---------------------------------------------------------------------------

# --- Web framework -------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Database ------------------------------------------------------------
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
# psycopg2-binary is only used by the synchronous migration helper and by
# tooling that expects a DBAPI2 driver (e.g. running init_schema.sql from
# Python). The API itself talks to Neon over asyncpg.
psycopg2-binary==2.9.10

# --- Validation / settings ----------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0

# --- HTTP + scraping -----------------------------------------------------
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
feedparser==6.0.11

# --- LLM extraction ------------------------------------------------------
google-generativeai==0.8.3

# --- Misc ----------------------------------------------------------------
python-dotenv==1.0.1
